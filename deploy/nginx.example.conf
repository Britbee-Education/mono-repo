# Example Nginx site map for BritBee on AIC Cloud VPS
#
#   britbee.app          → website/dist          (marketing)
#   app.britbee.app      → app/dist              (Expo web export)
#   api.britbee.app      → 127.0.0.1:3001
#   office.britbee.app   → 127.0.0.1:3003
#
# Copy snippets into /etc/nginx/sites-available/ and enable TLS with certbot.

# --- Marketing (Vite) ---
# server {
#   server_name britbee.app www.britbee.app;
#   root /home/ubuntu/britbee/website/dist;
#   index index.html;
#   location / { try_files $uri $uri/ /index.html; }
# }

# --- Kids / parent web app (Expo export) ---
# server {
#   server_name app.britbee.app;
#   root /home/ubuntu/britbee/app/dist;
#   index index.html;
#   location / { try_files $uri $uri/ /index.html; }
# }

# --- API ---
# server {
#   server_name api.britbee.app;
#   location / {
#     proxy_pass http://127.0.0.1:3001;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#     client_max_body_size 25m;
#   }
# }

# --- Office (backoffice) ---
# server {
#   server_name office.britbee.app;
#   location / {
#     proxy_pass http://127.0.0.1:3003;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#   }
# }
